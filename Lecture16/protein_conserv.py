#!/user/bin/python3

import os, sys
import numpy as np
import matplotlib.pyplot as plt

ecoli = open("alignment.txt").read()
array =  np.fromfile("alignment.txt")
np.transpose

#col_list = ecoli.split()
#columns = np.empty((len(ecoli[0]), len(ecoli)))
#for i in range(0, len(ecoli[0])):
#	columns[i] = []
#for lines in ecoli:
#	psrint(lines)
#	for amino in range(0, len(lines)):
#		columns[amino] = str(lines[amino]) 

#p)rint (columns[0])
	
	



